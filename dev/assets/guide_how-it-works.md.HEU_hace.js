import{_ as s,o as a,c as i,ao as t}from"./chunks/framework.u_sMZ99b.js";const u=JSON.parse('{"title":"How it works","description":"","frontmatter":{},"headers":[],"relativePath":"guide/how-it-works.md","filePath":"guide/how-it-works.md","lastUpdated":null}'),o={name:"guide/how-it-works.md"};function n(r,e,l,h,d,c){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="How-it-works" tabindex="-1">How it works <a class="header-anchor" href="#How-it-works" aria-label="Permalink to &quot;How it works {#How-it-works}&quot;">​</a></h1><p>A MEX file is a shared library exporting a single C entry point:</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> mexFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> nlhs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, mxArray </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">plhs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">[]</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> nrhs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, mxArray </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">prhs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">[]</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>Its <code>mx*</code>/<code>mex*</code> references (<code>mxCreateDoubleMatrix</code>, <code>mxGetPr</code>, <code>mexErrMsgIdAndTxt</code>, …) are <strong>undefined</strong> in the file — they resolve at load time from <code>libmx</code>/<code>libmex</code>. Unmex calls it in six steps.</p><h2 id="1.-Provide-the-symbols" tabindex="-1">1. Provide the symbols <a class="header-anchor" href="#1.-Provide-the-symbols" aria-label="Permalink to &quot;1. Provide the symbols {#1.-Provide-the-symbols}&quot;">​</a></h2><p><code>using Unmex</code> <code>dlopen</code>s the bundled host <code>libmxhost</code> with <code>RTLD_GLOBAL</code>. When you then <code>open_mex</code> a <code>.mex*</code>, the dynamic linker binds the MEX&#39;s undefined <code>mx*</code>/<code>mex*</code> symbols to the host — no MATLAB needed.</p><h2 id="2.-Marshal-inputs-Julia-prhs" tabindex="-1">2. Marshal inputs (Julia → <code>prhs</code>) <a class="header-anchor" href="#2.-Marshal-inputs-Julia-prhs" aria-label="Permalink to &quot;2. Marshal inputs (Julia → \`prhs\`) {#2.-Marshal-inputs-Julia-prhs}&quot;">​</a></h2><p>Each argument is converted to a freshly allocated <code>mxArray</code> by a per-class converter (see below) and collected into a <code>prhs</code> vector.</p><h2 id="3.-Invoke-through-an-error-shim" tabindex="-1">3. Invoke through an error shim <a class="header-anchor" href="#3.-Invoke-through-an-error-shim" aria-label="Permalink to &quot;3. Invoke through an error shim {#3.-Invoke-through-an-error-shim}&quot;">​</a></h2><p>Unmex never calls <code>mexFunction</code> directly. It calls a tiny C shim, <code>unmex_call</code>, that arms a <code>setjmp</code> target and then runs the MEX. A MEX&#39;s <code>mexErrMsgIdAndTxt</code> <code>longjmp</code>s back to that target instead of returning, so the error unwinds entirely within C and is reported to Julia as a return code + id/message — which <a href="/Unmex.jl/dev/reference/api#Unmex.call"><code>call</code></a> raises as an <code>ErrorException</code>. Without this, a MEX error would <code>abort()</code> the whole process.</p><h2 id="4.-Convert-outputs-plhs-Julia" tabindex="-1">4. Convert outputs (<code>plhs</code> → Julia) <a class="header-anchor" href="#4.-Convert-outputs-plhs-Julia" aria-label="Permalink to &quot;4. Convert outputs (\`plhs\` → Julia) {#4.-Convert-outputs-plhs-Julia}&quot;">​</a></h2><p>Mexicah knows its output types at compile time; Unmex does not — a returned <code>mxArray</code> is whatever the MEX produced. So <a href="/Unmex.jl/dev/reference/api#Unmex.mx_to_julia"><code>mx_to_julia</code></a> dispatches on the <strong>runtime</strong> <code>mxGetClassID</code> (plus the complex/sparse/cell/struct/ char flags) to pick the right Julia value.</p><h2 id="5.-Manage-lifetimes" tabindex="-1">5. Manage lifetimes <a class="header-anchor" href="#5.-Manage-lifetimes" aria-label="Permalink to &quot;5. Manage lifetimes {#5.-Manage-lifetimes}&quot;">​</a></h2><p>Unmex owns every <code>mxArray</code> it creates (the inputs) and the ones the MEX returns; it <code>mxDestroyArray</code>s them after conversion, and <code>GC.@preserve</code>s the Julia buffers across the <code>ccall</code>.</p><h2 id="The-converter-interface" tabindex="-1">The converter interface <a class="header-anchor" href="#The-converter-interface" aria-label="Permalink to &quot;The converter interface {#The-converter-interface}&quot;">​</a></h2><p>Conversions live in per-class converter types that implement the <code>AbstractMxConverter</code> <a href="https://github.com/el-oso/TypeContracts.jl" target="_blank" rel="noreferrer">TypeContracts</a> contract — <code>to_mx</code> (Julia → mxArray), <code>from_mx</code> (mxArray → Julia), and <code>mx_class_id</code>. The contract is verified structurally in the test suite, the same discipline Mexicah uses for its marshalers. Supporting a new MATLAB class is a matter of adding a converter type and registering it in the input/output dispatch tables.</p><h2 id="The-host-library" tabindex="-1">The host library <a class="header-anchor" href="#The-host-library" aria-label="Permalink to &quot;The host library {#The-host-library}&quot;">​</a></h2><p>The host (<code>cruntime/libmxhost.c</code>) is a from-scratch, MATLAB-free implementation of the libmx Matrix API. It is <strong>owned by LibMx</strong> — one canonical source shared with Mexicah&#39;s tests — and <code>deps/build.jl</code> compiles it into <code>runtime/</code>. Because an <code>mxArray</code> is opaque to the MEX — it only ever calls accessor functions — the host controls the struct layout internally, and a faithful set of <code>mx*</code>/<code>mex*</code> functions (memory, introspection, field and data accessors/mutators, strings) plus the versioned aliases (below) that MATLAB-compiled MEX link against is enough to serve a well-behaved MEX.</p><h2 id="Loading-a-real-MATLAB-compiled-MEX" tabindex="-1">Loading a real MATLAB-compiled MEX <a class="header-anchor" href="#Loading-a-real-MATLAB-compiled-MEX" aria-label="Permalink to &quot;Loading a real MATLAB-compiled MEX {#Loading-a-real-MATLAB-compiled-MEX}&quot;">​</a></h2><p>Two things differ between a hand-compiled C MEX and a genuine MATLAB <code>.mexa64</code>:</p><ol><li><p><strong>Versioned symbols.</strong> MATLAB versions its C API per release — a function is imported as <code>mxGetPr_800</code> (recent releases), <code>mxGetPr_730</code>, or <code>mxGetPr_700</code>, not bare <code>mxGetPr</code>. The host aliases <strong>every</strong> public function to all three suffixes, so <code>dlopen</code> with <code>RTLD_NOW</code> resolves them.</p></li><li><p><strong><code>DT_NEEDED</code>.</strong> A real MEX records a dependency on <code>libmx.so</code>/<code>libmex.so</code>, so the loader wants <em>files</em> by those names. The host is built under those <strong>sonames</strong> (<code>LibMx.build_libmxhost(...; soname=&quot;libmx.so&quot;)</code>) and <code>__init__</code> <code>dlopen</code>s both <code>RTLD_GLOBAL</code> — so a MEX&#39;s <code>DT_NEEDED</code> is satisfied by the already-loaded host, with no <code>LD_LIBRARY_PATH</code>.</p></li><li><p><strong>The C++ <code>mxArray</code> API.</strong> Modern MEX compiled from C++ import MATLAB&#39;s C++ API — mangled symbols like <code>matrix::detail::noninlined::mx_array_api::mxCreateDoubleMatrix</code> and <code>mxArray_tag</code> member functions — not just the C names. These are thin wrappers around the same C API, and Itanium mangling is deterministic, so the host ships a C++ layer (<code>cruntime/libmxhost_cpp.cpp</code>) that re-declares the namespaces/classes and <strong>forwards to the C functions</strong> (interpreter-coupled ones raise gracefully). LibMx compiles and links it into the host.</p></li></ol><p>A MEX (C or C++) that depends only on <code>libmx</code>/<code>libmex</code> (plus standard system libraries) therefore <strong>loads and runs</strong>. Verified end to end against a real MATLAB install: a point-in-polygon <code>.mexa64</code> returns results identical to an independent Julia implementation, and a C++ code-beautifier <code>.mexa64</code> called from Julia (no MATLAB) correctly reformats a C file. A MEX that also needs MATLAB&#39;s internal libraries (<code>libmwfl</code>, <code>libmwm_interpreter</code>, …) fails cleanly at <code>open_mex</code> with an undefined-library/symbol error.</p><div class="warning custom-block"><p class="custom-block-title">Calling an unknown MEX is unsafe</p><p>A MEX trusts its inputs — calling one with the wrong arity/types can dereference a missing <code>prhs[i]</code> and <strong>segfault</strong> (the <code>setjmp</code> shim catches a <code>longjmp</code>, not a signal). For an unfamiliar binary, read its docs/source for the signature, or use <a href="/Unmex.jl/dev/reference/api#Unmex.probe"><code>probe</code></a> with <code>call=false</code> (scans without invoking it).</p></div><h2 id="Interpreter-only-MEX" tabindex="-1">Interpreter-only MEX <a class="header-anchor" href="#Interpreter-only-MEX" aria-label="Permalink to &quot;Interpreter-only MEX {#Interpreter-only-MEX}&quot;">​</a></h2><p>Some MEX call back into the MATLAB interpreter (<code>mexCallMATLAB</code> for a real builtin, <code>mexEvalString</code>, <code>mexGetVariable</code>, …). The host can&#39;t fabricate those without a live MATLAB, but it fails <strong>gracefully</strong> rather than crashing:</p><ul><li><p>The interpreter symbols are present in the host (so <code>dlopen</code> with <code>RTLD_NOW</code> resolves them and the MEX loads), but each <strong>raises a catchable Julia <code>ErrorException</code></strong> when actually called — e.g. <code>mexEvalString needs a live MATLAB interpreter</code>.</p></li><li><p>The host&#39;s <code>mexCallMATLAB</code> <strong>raises</strong> for any builtin other than the emulated <code>&quot;string&quot;</code>/<code>&quot;cellstr&quot;</code> (instead of returning an error code), so a MEX that ignores the return value and dereferences the output can&#39;t segfault the process.</p></li><li><p>A MEX that <em>returns</em> a function handle / classdef object / opaque type fails with a clear &quot;unsupported output class&quot; error.</p></li></ul><p>The one remaining hard-crash path is a MEX that calls <code>mexCallMATLAB</code> and then derefs its (untouched) output <strong>without</strong> the call unwinding first — which the host&#39;s raise now prevents in the common case. A MEX that references an <code>mx*</code> accessor the host doesn&#39;t implement yet fails cleanly at <code>open_mex</code> with an undefined-symbol error.</p><h2 id="The-call-signature-is-opaque" tabindex="-1">The call signature is opaque <a class="header-anchor" href="#The-call-signature-is-opaque" aria-label="Permalink to &quot;The call signature is opaque {#The-call-signature-is-opaque}&quot;">​</a></h2><p>A <code>.mex*</code> exports exactly one C symbol — <code>mexFunction(nlhs, plhs, nrhs, prhs)</code> — and carries <strong>no signature metadata</strong>: no argument count, no types, nothing in the symbol table but <code>mexFunction</code>. The &quot;signature&quot; exists only as the runtime checks inside that function (<code>if (nrhs != 1) …</code>, <code>mxIsDouble(prhs[0])</code>, …) and in whatever docs the author shipped. You recover it indirectly: read the author&#39;s <code>.m</code> help / source if you have it, run <code>strings</code> over the binary (the <code>mexErrMsgIdAndTxt</code> texts often spell out the contract), or <strong>probe it</strong> — call with trial inputs and read the catchable errors.</p><p><a href="/Unmex.jl/dev/reference/api#Unmex.probe"><code>probe</code></a> automates the last two: it sweeps input arities, tries a few argument types at the smallest arity that works, and scans the binary for contract strings — reporting what it finds. Because a MEX can be polymorphic (branching on count, type, or a leading command string), treat the result as discovery, not a guaranteed spec.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;" class="repl-prompt repl-prompt-julia">julia</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;" class="repl-prompt repl-prompt-julia">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> probe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;double_it.mexa64&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Probing </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;double_it.mexa64&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  (entry point</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mexFunction)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Arity sweep (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">×</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Matrix{Float64} per argument)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  nargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> →</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MEX raised [Unmex</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">double_it</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">nargin]</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> double_it expects exactly </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> input</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  nargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> →</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> OK, returns Float64 </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">×</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  …</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Embedded contract strings (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  double_it expects exactly </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> input</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Unmex</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">double_it</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">nargin</span></span></code></pre></div>`,31)])])}const k=s(o,[["render",n]]);export{u as __pageData,k as default};
